<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="BaseTheme"
        parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
        <!--<item name="alertDialogTheme">@style/Theme.AppCompat.Dialog</item>-->
        <item name="alertDialogTheme">@style/AppCompatAlertDialogStyle</item>

        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:textColorTertiary">@color/text_tertiary</item>
        <item name="android:textColorPrimaryInverse">@color/text_primary_inverse</item>
        <item name="android:textColorSecondaryInverse">@color/text_primary_inverse</item>
        <item name="android:textColorTertiaryInverse">@color/text_tertiary_inverse</item>
    </style>

    <style name="MainTheme"
        parent="BaseTheme"/>

    <style name="ToolbarBase">
        <item name="android:theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:background">@drawable/toolbar_background_disabling</item>
    </style>

    <style name="Toolbar"
        parent="ToolbarBase"/>

    <!--TODO continue testing on nexus s to see if theme works on dialog-->
    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">?attr/colorPrimary</item>
        <item name="colorPrimaryDark">?attr/colorPrimaryDark</item>
        <item name="colorAccent">?attr/colorAccent</item>
    </style>

</resources>
